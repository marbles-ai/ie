To build the Docker image, run: 'docker build -t vpython .'

It won't work right now, because the environmental variables 'PYTHON_VERSION' and 'PYTHON_BUILD' are not set in the container. Set these first
